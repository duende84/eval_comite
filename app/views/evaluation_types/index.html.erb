<div id="reg_form" class="bg_form">
  <h1>Listado Tipos de Evaluacion</h1>
  <table>
    <tr>
      <th>Nombre</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  <% @evaluation_types.each do |evaluation_type| %>
    <tr>
      <td><%= evaluation_type.name %></td>
      <td><%= link_to 'mostrar', evaluation_type %></td>
      <td><%= link_to 'Editar', edit_evaluation_type_path(evaluation_type) %></td>
      <td><%= link_to 'Eliminar', evaluation_type, method: :delete, data: { confirm: '¿Esta seguro?' } %></td>
    </tr>
  <% end %>
  </table>
  <hr>
  <%= link_to 'Ingresar Nuevo', new_evaluation_type_path, class: "btn btn-primary btn_form" %>
</div>