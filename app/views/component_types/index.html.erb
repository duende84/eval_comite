<div id="reg_form" class="bg_form">
 <h1>Listado Tipos de Componentes</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Descrripcion</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @component_types.each do |component_type| %>
  <tr>
    <td><%= component_type.name %></td>
    <td><%= component_type.description %></td>
    <td><%= link_to 'Mostrar', component_type %></td>
    <td><%= link_to 'Editar', edit_component_type_path(component_type) %></td>
    <td><%= link_to 'Eliminar', component_type, method: :delete, data: { confirm: '¿Esta seguro?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Tipo Componente', new_component_type_path, class: "btn btn-primary btn_form" %>
</div>