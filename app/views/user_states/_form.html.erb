<%= form_for(@user_state) do |f| %>
  <% if @user_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_state.errors.count, "error") %> prohibited this user_state from being saved:</h2>

      <ul>
      <% @user_state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :name,"Nombre",:class => "label_form"%>
  <%= f.text_field :name,:class => "text_field_form", :required => true %>

  <%= f.submit "Guardar", class: "btn btn-primary btn_form" %>
  <hr>
<% end %>