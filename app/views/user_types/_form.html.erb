<%= form_for(@user_type) do |f| %>
  <% if @user_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_type.errors.count, "error") %> prohibited this user_type from being saved:</h2>

      <ul>
      <% @user_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Nombre", :class => "label_form" %><br />
    <%= f.text_field :name, :class => "text_field_form", :required => true %>
  </div>s
  <div class="field">
    <%= f.label :description, "Descripcion", :class => "label_form" %><br />
    <%= f.text_field :description, :class => "text_field_form" %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar", class: "btn btn-primary btn_form" %>
  </div>
  <hr>
<% end %>
