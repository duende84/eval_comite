<div id="reg_form" class="bg_form">
  <h1>Listado Tipos de Usuario</h1>
  <table>
    <tr>
      <th>Nombre</th>
      <th>Descripcion</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  <% @user_types.each do |user_type| %>
    <tr>
      <td><%= user_type.name %></td>
      <td><%= user_type.description %></td>
      <td><%= link_to 'Mostrar', user_type %></td>
      <td><%= link_to 'Editar', edit_user_type_path(user_type) %></td>
      <td><%= link_to 'Eliminar', user_type, method: :delete, data: { confirm: '¿Estas seguro?' } %></td>
    </tr>
  <% end %>
  </table>
  <hr>
  <%= link_to 'Ingresar Nuevo', new_user_type_path, class: "btn btn-primary btn_form" %>
</div>