<%= form_for(@formulary) do |f| %>
  <% if @formulary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@formulary.errors.count, "error") %> prohibited this formulary from being saved:</h2>

      <ul>
      <% @formulary.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
